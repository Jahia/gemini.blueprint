<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>gemini-blueprint</artifactId>
        <groupId>org.eclipse.gemini.blueprint</groupId>
        <version>1.0.2.RELEASE</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <version>1.0.2.RELEASE-jahia3-SNAPSHOT</version>
    <groupId>org.eclipse.gemini.blueprint</groupId>
    <artifactId>gemini-blueprint-core</artifactId>
    <packaging>jar</packaging>
    <url>http://www.eclipse.org/gemini/blueprint/</url>
    <name>Gemini Blueprint Core</name>

    <!-- The parent points at dav.codehaus.org and svn.codehaus.org, and neither host exists.
         Only this module carries a Jahia version, so only this module is published. -->
    <scm>
        <connection>scm:git:https://github.com/Jahia/gemini.blueprint.git</connection>
        <developerConnection>scm:git:git@github.com:Jahia/gemini.blueprint.git</developerConnection>
        <url>https://github.com/Jahia/gemini.blueprint</url>
        <tag>HEAD</tag>
    </scm>

    <distributionManagement>
        <repository>
            <id>thirdparty-releases</id>
            <name>Jahia Third-party Releases</name>
            <url>https://devtools.jahia.com/nexus/content/repositories/thirdparty-releases</url>
        </repository>
        <snapshotRepository>
            <id>thirdparty-snapshots</id>
            <name>Jahia Third-party Snapshots</name>
            <url>https://devtools.jahia.com/nexus/content/repositories/thirdparty-snapshots</url>
        </snapshotRepository>
    </distributionManagement>

    <description>
        Eclipse Gemini Blueprint core classes. Offers OSGi-based application context and importer/exporter
        functionality.
    </description>

    <properties>
        <javadoc.loc>${basedir}/../docs/src/javadoc</javadoc.loc>
        <spring.osgi.symbolic.name>${symName.prefix}.core</spring.osgi.symbolic.name>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.eclipse.gemini.blueprint</groupId>
            <artifactId>gemini-blueprint-io</artifactId>
            <version>1.0.2.RELEASE</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.gemini.blueprint</groupId>
            <artifactId>gemini-blueprint-mock</artifactId>
            <version>1.0.2.RELEASE</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-beans</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>aopalliance</groupId>
            <artifactId>aopalliance</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.osgi</groupId>
           	<artifactId>org.osgi.compendium</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>multithreadedtc</groupId>
            <artifactId>multithreadedtc</artifactId>
            <version>1.01</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>backport-util-concurrent</groupId>
                    <artifactId>backport-util-concurrent</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl-over-slf4j</artifactId>
            <version>1.6.4</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <configuration>
                    <instructions>
                        <Export-Package>
                            !*internal*,
                            org.eclipse.gemini.blueprint.*;version="1.0.2.RELEASE",
                            org.osgi.service.blueprint.container;version="1.0.0",
                            org.osgi.service.blueprint.reflect;version="1.0.0"
                        </Export-Package>
                        <!-- BND imports what a bundle exports, and bundlor did not. The two
                             exclusions below keep the import list the released bundle carries. -->
                        <Import-Package>
                            org.eclipse.gemini.blueprint.io;version="[1.0.2.RELEASE,1.0.2.RELEASE]",
                            !org.eclipse.gemini.blueprint.*,
                            !org.osgi.service.blueprint,
                            org.osgi.service.blueprint.container;version="[1.0.0,1.1)",
                            org.osgi.service.blueprint.reflect;version="[1.0.0,1.1)",
                            org.springframework.aop;version="${spring.version.range.nq}",
                            org.springframework.beans.propertyeditors;version="${spring.version.range.nq}",
                            org.osgi.framework;version="1.3",
                            org.aopalliance.*;version="0.0.0",
                            org.osgi.service.cm;version="1.2";resolution:=optional,
                            org.osgi.service.packageadmin;version="1.2",
                            org.osgi.service.startlevel;version="1.0";resolution:=optional,
                            org.apache.commons.logging.*;version=${logging.version.range},
                            org.springframework.*;version=${spring.version.range},
                            *;resolution:=optional
                        </Import-Package>
                    </instructions>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
